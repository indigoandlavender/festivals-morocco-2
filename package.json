{
  "name": "morocco-events-search",
  "version": "1.0.0",
  "description": "Vertical search engine for festivals and music events in Morocco",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build needed - Vercel handles API routes'",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
